{
  "name": "feature-payments",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "feature-payments/src",
  "prefix": "p4",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/angular:ng-packagr-lite",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "feature-payments/ng-package.json",
        "tsConfig": "feature-payments/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "feature-payments/tsconfig.lib.prod.json"
        },
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "feature-payments/jest.config.ts",
        "tsConfig": "feature-payments/tsconfig.spec.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}

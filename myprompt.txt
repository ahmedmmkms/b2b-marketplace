System / Role:
You are a senior full-stack engineer. Your task is to replace an existing Angular 18 + Nx frontend (“frontend2/”) with a production-ready React stack. Keep the existing Spring Boot 3 / Java 21 backend as-is and integrate with it via OpenAPI-generated clients.

Context & Requirements (from project spec):
Build a VAT-ready, multi-vendor B2B marketplace for GCC/MENA with: Catalog & search, RFQ → Quote → Compare → PO/Order, Payments & Corporate Wallet, VAT invoicing & credit notes, Loyalty (tiers, earn/burn), Identity/Access, Observability, Feature Flags. Dual language English/Arabic with full RTL across all user-facing surfaces. Modules/domains include Catalog, Search, RFQ/Quote, Orders, Payments, Invoicing/VAT, Wallet/Credit, Loyalty, Identity/Access, Ops/Audit, Shared-Kernel. 

QWEN

High-level goals:

Delete/retire the Angular workspace and generate a Next.js 14 (App Router) + TypeScript frontend.

Set Node.js = 22.x (engines & CI).

Implement all features above with EN/AR + RTL and robust a11y.

Integrate with Spring Boot APIs via OpenAPI clients; keep feature flags and observability.

Ready for Cloudflare Pages deploy (GitHub CI, PR previews).

Tech stack (use these unless a better default is essential):

Framework: Next.js 14 (App Router), React 18, TypeScript.

Styling/UI: Tailwind CSS + tailwindcss-rtl (logical properties), shadcn/ui components, Radix UI primitives.

State/data: TanStack Query (server caching), Zustand (light client state), Zod (schema validation).

i18n/RTL: next-intl or next-i18next with locales en, ar; automatic dir switching (dir="ltr|rtl"), number/date formatting via Intl; Arabic collation for sorting.

Forms: React Hook Form + Zod resolver.

Dates: Day.js with locales (en, ar).

Auth: JWT/OAuth2 bearer support compatible with Spring Security; pluggable provider.

OpenAPI client: Generate per backend module from exposed OpenAPI docs (one client per domain: catalog, rfq, quotes, orders, payments, invoicing, wallet, loyalty, identity).

Feature flags: lightweight JSON/env-driven flags; easy to swap to Unleash/Flagsmith later.

Testing: Vitest + React Testing Library; Playwright for E2E.

Lint/format: ESLint, Prettier, TypeScript strict.

CI/CD: GitHub Actions (install Node 22, pnpm, build, test, type-check; deploy to Cloudflare Pages).

Packaging: pnpm.

Observability: Log request/response metadata, expose build SHA, commit link, and feature flags in a diagnostics page.

Migration directives (very important):

Remove Angular/Nx code under frontend2/ and references in CI.

Create a new top-level frontend/ app (React).

Keep backend repo untouched; only consume its APIs.

Provide a readme section “Angular → React migration notes” summarizing what changed and how to run both ends locally.

App capabilities & pages (minimum):

Public Catalog: browse categories, product detail, media gallery; search (initially proxied to Postgres FTS).

Account/Identity: sign-in/up, company profile, roles/permissions surfaces.

RFQ/Quote: create RFQ, vendor quote submission (vendor console), quote compare, accept/decline.

Orders & Payments: convert accepted quote → PO/Order; payment initiation; basic Corporate Wallet view.

Invoicing/VAT: invoice list/detail, sequence display, PDF download hook, credit notes.

Wallet/Credit: wallet balance/transactions, credit limits.

Loyalty: show points, tiers, earn/burn history.

Admin/Ops: feature flags toggle (local only), audit log viewer (read-only).

Language toggle: site-wide language switch (EN/AR) that also flips RTL/LTR dynamically and persists per user.

Accessibility: focus order, keyboard nav, color-contrast tokens, semantic landmarks.

API integration details:

Create .env.local variables for backend base URLs per module.

Generate OpenAPI SDKs into ./frontend/libs/api/<module> and re-generate via pnpm api:generate.

Use TanStack Query for all data fetching with proper caching keys per domain/entity.

Use cursor/keyset pagination and ULIDs per API guidance. 

QWEN

Directory structure (create):

frontend/
  app/                      # Next.js App Router
    (public)/
      catalog/[…slug]/page.tsx
      product/[id]/page.tsx
    account/…
    rfq/…
    quotes/…
    orders/…
    payments/…
    invoices/…
    wallet/…
    loyalty/…
    admin/…
    layout.tsx
    page.tsx
  components/
  features/                 # per-domain feature folders
  libs/
    api/                    # OpenAPI-generated clients per module
    i18n/                   # i18n config, messages
    ui/                     # shared UI widgets
  public/
  styles/
  tests/
  next.config.mjs
  tailwind.config.ts
  package.json


Internationalization/RTL tasks:

Provide messages/en.json and messages/ar.json with keys for navigation, catalog, RFQ, orders, invoices, wallet, loyalty, errors.

Implement <LanguageSwitcher> that flips locale + dir.

Ensure logical CSS properties (margin-inline, padding-inline, start/end) and icon mirroring where appropriate.

Sorting/formatting uses locale-aware Intl.Collator('ar') when Arabic is active.

Node & tooling:

package.json → "engines": { "node": ">=22.0.0" }.

Add scripts:

"dev", "build", "start", "lint", "typecheck", "test", "e2e", "api:generate".

Generate a GitHub Actions workflow: Node 22, pnpm cache, lint/test/build, upload artifact; on main push, deploy to Cloudflare Pages (env placeholders).

Feature flags (initial register):
catalog.publicBrowse, search.enabled, rfq.enabled, quote.vendorConsole, orders.checkout, payments.gateway1, wallet.basic, invoice.vat, loyalty.core, credit.controls. Load from process.env/JSON at runtime. 

QWEN

Deliverables to produce now:

New frontend/ Next.js project scaffold with all configs above.

Minimal vertical slices for: Catalog list + details, RFQ create, Quote compare, Order draft, Invoice list, Wallet view, Loyalty summary.

README.md with run instructions, environment variables, and Angular→React migration notes.

api:generate script and example OpenAPI config to point at backend specs.

GitHub Actions workflow and Cloudflare Pages config snippet.

Example EN/AR message files and a working Language Switcher that demonstrates RTL flipping.

Constraints & quality bar:

Performance targets from the spec (e.g., p95 search < 500 ms; checkout median < 2 s) inform UI loading states and prefetching.

OWASP ASVS L2 mindset; never leak secrets; structured error handling with RFC7807 mapping. 

QWEN

Ship with accessible components (labels, roles, focus states).

Code must be clean, typed, and production-grade.
import { AppShell } from '@/components/common/app-shell';
import { SupplierQuoteInbox } from './supplier-quote-inbox';

export default function SupplierQuotesInboxPage() {
  return (
    <AppShell>
      <SupplierQuoteInbox />
    </AppShell>
  );
}

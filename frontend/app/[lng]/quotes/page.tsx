import { AppShell } from '@/components/common/app-shell';
import { QuoteInbox } from './quote-inbox';

export default function QuotesPage() {
  return (
    <AppShell>
      <QuoteInbox />
    </AppShell>
  );
}
